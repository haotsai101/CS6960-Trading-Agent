Tasks to complete.

Based on 00-OVERVIEW.md
1. 00-OVERVIEW.md referencing that current portfolio uses
`max_positions` | `300` | Position count capacity |
TODO:  Portfolio doesn't have such restrictions. Remove any reference in documentation. Analyze the front end and remove any references to `max_positions`.

2. Inception date is `2024-09-18`. The earliest transaction.
TODO: Update any references.

3. Factor loading data could be hard to optain. `factor_ceiling` is part of portfolio settings. Mark as `Not a priority`. 

4. Data refresh: Daily after hours (look into Alpha Advantage API limits, explore other options if possible)